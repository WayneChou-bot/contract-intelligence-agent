"""Reusable, framework-agnostic capabilities ("skills").

Each skill is a plain Python package with no dependency on the agent runtime,
so it can be called by an ADK agent today, or any other framework tomorrow.
"""
